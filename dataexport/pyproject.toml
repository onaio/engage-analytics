[project]
name = "engage-dataexport"
version = "0.1.0"
description = "Export engage analytics data to S3"
requires-python = ">=3.10"
dependencies = [
    "psycopg2-binary>=2.9.7",
    "python-dotenv>=1.0.0",
    "boto3>=1.34.0",
]

[project.scripts]
export-to-s3 = "export_to_s3:main"
